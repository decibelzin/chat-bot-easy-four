# Copie para .env e preencha com seus valores (nunca commite .env)

# Token que você definiu no painel ao configurar o Webhook (Verify Token)
VERIFY_TOKEN=seu_verify_token_aqui

# Chave secreta do aplicativo (Configurações do app > Básico)
APP_SECRET=sua_chave_secreta_do_app

# Em desenvolvimento: use 1 para ignorar validação da assinatura (evita 401). Produção: remova ou 0
SKIP_WEBHOOK_SIGNATURE=1

# Page Access Token (token que você gerou em "Adicionar conta" no Instagram)
PAGE_ACCESS_TOKEN=seu_page_access_token_aqui

# Porta do servidor (use 3000 com npm run tunnel; ngrok usa essa porta)
PORT=3000
